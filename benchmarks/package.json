{
  "name": "@prompt-os/benchmarks",
  "private": true,
  "type": "module",
  "scripts": {
    "bench:cache": "bun run cache-latency.ts",
    "bench:compression": "bun run compression-throughput.ts",
    "bench:tokens": "bun run token-estimation-accuracy.ts",
    "bench:classifier": "bun run classifier-speed.ts",
    "bench:all": "bun run bench:cache && bun run bench:compression && bun run bench:tokens && bun run bench:classifier"
  },
  "dependencies": {
    "@prompt-os/shared": "workspace:*",
    "@prompt-os/sdk": "workspace:*",
    "@prompt-os/router": "workspace:*"
  }
}
