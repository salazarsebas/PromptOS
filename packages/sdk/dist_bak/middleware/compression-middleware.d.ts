import type { CompressionConfig, Middleware } from '@promptos/shared';
export declare function createCompressionMiddleware(
  config: CompressionConfig,
  maxTokens?: number,
): Middleware;
//# sourceMappingURL=compression-middleware.d.ts.map
